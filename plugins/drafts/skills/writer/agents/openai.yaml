interface:
  display_name: "Writer"
  short_description: "Draft, revise, and transform writing"
  brand_color: "#2563EB"
  default_prompt: "Use $writer to create a brief, document contract, draft, revision, or channel variant."

policy:
  allow_implicit_invocation: true
