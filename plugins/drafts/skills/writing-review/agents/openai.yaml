interface:
  display_name: "Writing Review"
  short_description: "Review draft versions and prioritize revisions"
  brand_color: "#2563EB"
  default_prompt: "Use $writing-review to critique a draft version, or to give clearly labeled one-off advice when no version exists."

policy:
  allow_implicit_invocation: false
