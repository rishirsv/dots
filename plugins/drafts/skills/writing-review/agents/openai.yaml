interface:
  display_name: "Writing Review"
  short_description: "Review draft versions and plan fixes"
  brand_color: "#2563EB"
  default_prompt: "Use $writing-review to critique a specific draft version and produce findings or a scorecard."

policy:
  allow_implicit_invocation: true
