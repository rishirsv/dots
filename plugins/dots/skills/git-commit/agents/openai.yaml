interface:
  display_name: "Git Commit"
  short_description: "Create clean local git commits"
  icon_small: "../../assets/icon.png"
  icon_large: "../../assets/logo.png"
  brand_color: "#3B82F6"
  default_prompt: "Use $git-commit to stage the intended local changes and create a clean git commit; push only when I explicitly say to push."

policy:
  allow_implicit_invocation: false
