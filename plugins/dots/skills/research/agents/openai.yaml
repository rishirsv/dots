interface:
  display_name: "Research"
  short_description: "Research code, docs, and web sources"
  default_prompt: "Use $research to investigate this question, gather evidence, and recommend the right next step."

policy:
  allow_implicit_invocation: false
