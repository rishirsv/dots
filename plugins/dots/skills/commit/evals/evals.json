{
  "schema_version": 2,
  "skill_name": "commit",
  "target": {
    "type": "skill",
    "ref": "SKILL.md"
  },
  "defaults": {
    "runner": "codex_app_server",
    "repetitions": 1
  },
  "evals": [
    {
      "id": "staged-fast-path",
      "type": "capability",
      "prompt": "Commit the staged formatter fix only. Do not push.",
      "expected_output": "One focused local commit containing only the staged formatter fix, with validation and commit details reported and no push.",
      "expectations": [
        "Inspects the staged diff and runs staged whitespace validation before committing.",
        "Preserves unrelated unstaged or untracked work.",
        "Creates one local commit with a repository-appropriate message.",
        "Reports the commit SHA, subject, and validation evidence without pushing."
      ]
    },
    {
      "id": "mixed-worktree-scope",
      "type": "gate",
      "prompt": "Commit only the documentation edits. There are unrelated app changes in the worktree.",
      "expected_output": "A focused documentation commit that preserves unrelated app changes and asks only if safe staging cannot separate the intended scope.",
      "expectations": [
        "Stages only intended docs paths or asks if hunk separation is unsafe.",
        "Does not use broad staging that would sweep unrelated app changes into the commit.",
        "Keeps the commit focused on one reviewer-visible story.",
        "Mentions leftover work only if it affects the next step."
      ]
    },
    {
      "id": "message-only",
      "type": "capability",
      "prompt": "Write a commit message for the staged diff, but do not create the commit.",
      "expected_output": "A repository-style commit message derived from the staged diff without changing the index or creating a commit.",
      "expectations": [
        "Inspects the staged diff enough to understand scope.",
        "Returns a concise commit subject and optional body when useful.",
        "Does not run git commit or mutate the index.",
        "Follows the repository's recent message style when it is visible."
      ]
    },
    {
      "id": "commit-and-push",
      "type": "gate",
      "prompt": "Commit the intended change and push the branch.",
      "expected_output": "A focused local commit followed by a safe branch push after remote and upstream checks, without opening a PR or rewriting history.",
      "expectations": [
        "Creates the local commit first with focused scope.",
        "Checks branch, upstream, default branch, and remote state before pushing.",
        "Sets upstream tracking only for a clear feature or work branch.",
        "Does not open a PR, merge, or force-push without explicit approval."
      ]
    }
  ]
}
