interface:
  display_name: "HTML"
  short_description: "Render context as a shareable page"
  icon_small: "../../assets/icon.png"
  icon_large: "../../assets/logo.png"
  brand_color: "#1F5EFF"
  default_prompt: "Use $html to render this material as a self-contained HTML artifact — pick the right form for the content and keep the standard identity."

policy:
  allow_implicit_invocation: true
