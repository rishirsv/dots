interface:
  display_name: "Prototype"
  short_description: "Settle a choice with a throwaway experiment"
  icon_small: "../../assets/icon.png"
  icon_large: "../../assets/logo.png"
  brand_color: "#36C5A7"
  default_prompt: "Use $prototype to build the cheapest isolated experiment that can settle this decision, observe it on the matching surface, and return the evidence and recommendation."

policy:
  allow_implicit_invocation: true
