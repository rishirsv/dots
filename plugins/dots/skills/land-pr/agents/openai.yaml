interface:
  display_name: "Land PR"
  short_description: "Merge a ready PR safely"
  icon_small: "../../assets/icon.png"
  icon_large: "../../assets/logo.png"
  brand_color: "#3B82F6"
  default_prompt: "Use $land-pr to merge the current PR only if its checks, reviews, conversations, and head SHA are safe."

policy:
  allow_implicit_invocation: false
