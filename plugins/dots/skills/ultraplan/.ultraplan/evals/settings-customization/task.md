Create a comprehensive implementation plan for reorganizing an app's Settings
and adding the customization work described by the verified repository
snapshot.

Treat `fixtures/repo-context.md` as the source of truth. Plan all requested
work, not only the easiest first release, while preserving the named ownership,
privacy, persistence, and external-system boundaries. Plan only: do not
implement or modify the fixture.

Save the durable plan artifact or artifacts under `artifacts/plans/`, then
return a concise handoff. Another capable repository agent will inspect the
source during implementation, so keep the output executable without repeating
the repository snapshot as a report. Split independently approvable work when
that produces safer, more reviewable plans.
