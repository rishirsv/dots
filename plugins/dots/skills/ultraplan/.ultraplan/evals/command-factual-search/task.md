Create an implementation plan for adding bounded factual answers to an app's
global Command surface and exposing the same query through system search.

Treat `fixtures/repo-context.md` as a verified snapshot of the repository.
Ground the plan in those paths, symbols, owners, and constraints. Plan only:
do not implement or modify the fixture. Save the durable plan artifact or
artifacts under `artifacts/plans/`, then return a concise handoff.

The result must be safe for another capable repository agent to implement. It
must preserve the trust boundaries around cached health data, exact evidence,
ambiguous questions, and active-workout identity without turning the plan into
a research report or repository tutorial.
