{
  "schema_version": 1,
  "skill_name": "debug",
  "defaults": {
    "runner": "codex_app_server",
    "repetitions": 3
  },
  "candidates": [
    {
      "candidate": "current",
      "display": "Current skill",
      "source": {
        "kind": "current_worktree",
        "ref": "."
      }
    }
  ],
  "cases": [
    {
      "id": "trigger-regression-unknown-cause",
      "type": "trigger",
      "expectations": [
        "The agent selects debug and reproduces the failure before proposing causes.",
        "A regression strategy (history bisection or last-known-good comparison) is considered."
      ],
      "task": {
        "prompt": "The nightly export job started failing sometime last week — figure out why."
      }
    },
    {
      "id": "trigger-ci-only-failure",
      "type": "trigger",
      "expectations": [
        "The agent selects debug and isolates environment differences one variable at a time.",
        "Conclusions without a reproduction are labeled likely or needs-testing."
      ],
      "task": {
        "prompt": "Why does this test pass locally but fail in CI?"
      }
    },
    {
      "id": "trigger-error-message",
      "type": "trigger",
      "expectations": [
        "The agent selects debug, captures the exact error verbatim, and traces the origin rather than the last symptom."
      ],
      "task": {
        "prompt": "npm run build suddenly throws \"TypeError: cannot read properties of undefined\" — can you look into it?"
      }
    },
    {
      "id": "nearmiss-known-fix",
      "type": "near_miss",
      "expectations": [
        "The agent does not select debug; the cause is already diagnosed and this is plain implementation."
      ],
      "task": {
        "prompt": "Apply the fix we discussed for the null pointer in the parser."
      }
    },
    {
      "id": "nearmiss-flaky-rerun",
      "type": "near_miss",
      "expectations": [
        "The agent does not select debug; the user asked for a rerun of known-flaky tests, not a diagnosis."
      ],
      "task": {
        "prompt": "The integration tests are flaky again — just rerun them."
      }
    }
  ]
}
