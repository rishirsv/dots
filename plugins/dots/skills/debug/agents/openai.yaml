interface:
  display_name: "Debug"
  short_description: "Evidence-first root-cause diagnosis for bugs, failing tests, and regressions"
  icon_small: "../../assets/icon.png"
  icon_large: "../../assets/logo.png"
  brand_color: "#3B82F6"
  default_prompt: "Use $debug to reproduce this failure, isolate the cause, and verify the root cause before fixing."
policy:
  allow_implicit_invocation: true
