interface:
  display_name: "handoff"
  short_description: "Create a clear continuation handoff"
  icon_small: "../../assets/icon.png"
  icon_large: "../../assets/logo.png"
  brand_color: "#3B82F6"
  default_prompt: "Use $handoff to create a continuation brief with current state, decisions, risks, validation, and next actions. Reference existing task or plan artifacts instead of duplicating them."

policy:
  allow_implicit_invocation: false
