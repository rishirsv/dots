interface:
  display_name: "Handoff"
  short_description: "Create a clear continuation handoff"
  icon_small: "../../assets/icon.png"
  icon_large: "../../assets/logo.png"
  brand_color: "#3B82F6"
  default_prompt: "Use $handoff to create a concise continuation brief with current state, decisions, risks, and next step. Reference existing Work Tracker items or Plans instead of duplicating them. If requested, use the available platform tools for a new session, fork, or isolated workspace continuation."

policy:
  allow_implicit_invocation: false
