interface:
  display_name: "GC"
  short_description: "Create clean local git commits"
  icon_small: "../../assets/icon.png"
  icon_large: "../../assets/logo.png"
  brand_color: "#3B82F6"
  default_prompt: "Use $gc to stage the intended local changes and create a clean git commit; use $gc push when I explicitly want it pushed too."

policy:
  allow_implicit_invocation: false
