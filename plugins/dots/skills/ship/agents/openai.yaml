interface:
  display_name: "Ship PR"
  short_description: "Publish scoped work as a PR"
  icon_small: "../../assets/icon.png"
  icon_large: "../../assets/logo.png"
  brand_color: "#3B82F6"
  default_prompt: "Use $ship to publish the scoped local change as a pushed branch and PR; request review only when I explicitly ask."

policy:
  allow_implicit_invocation: true
