interface:
  display_name: "pulse"
  short_description: "Read current social and market signal"
  default_prompt: "Use $pulse to find what people are saying, where views differ, and how strong the evidence is."
policy:
  allow_implicit_invocation: true
