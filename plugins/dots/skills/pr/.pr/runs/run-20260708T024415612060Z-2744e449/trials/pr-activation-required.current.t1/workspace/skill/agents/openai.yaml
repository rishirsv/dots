interface:
  display_name: "PR"
  short_description: "Publish local changes as a GitHub PR"
  icon_small: "../../assets/icon.png"
  icon_large: "../../assets/logo.png"
  brand_color: "#3B82F6"
  default_prompt: "Use $pr to confirm scope, commit local changes, push the branch, and open a draft GitHub PR. Use it for review-comment repair only when explicitly requested."

policy:
  allow_implicit_invocation: true
