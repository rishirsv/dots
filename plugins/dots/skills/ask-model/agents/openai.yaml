interface:
  display_name: "Ask Model"
  short_description: "Consult or delegate to a CLI model with repo access"
  icon_small: "../../assets/icon.png"
  icon_large: "../../assets/logo.png"
  brand_color: "#36C5A7"
  default_prompt: "Use $ask-model to assign a command-line model the requested role—consultation, implementation, or review—and keep ownership explicit through completion."

policy:
  allow_implicit_invocation: false
