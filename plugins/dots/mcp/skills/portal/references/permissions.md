# Permissions

Roots and capability families are granted by the local owner. OAuth scopes further restrict hosted requests. Shared workspace handles coordinate same-owner callers, not cryptographically separate chats.
