# Shell And Env

Pass argv exactly. Shell interpretation requires an explicit shell argv. Login startup files, loader injection, API credentials and control settings are not accepted environment overrides. Command network permission is boolean, not host-filtered.
