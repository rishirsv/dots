# Apple Development

Qualify a disposable Swift package before Xcode. Approve toolchain/cache roots locally. Run `swift test` and `xcodebuild` against fixtures with signing disabled. Physical Mac sleep/wake, PTY and descendant teardown tests are required before claiming this profile.
