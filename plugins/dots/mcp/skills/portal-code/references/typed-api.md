# Typed Api

Call describe with the selected capability IDs. Signatures are generated from the same schemas used by the broker. Guest calls omit workspaceId and writerEpoch; mutating calls add stepKey. No generic RPC or module loader is exposed.
