# Synthetic test fixtures

All content is generated for testing. Copy fixtures into a disposable granted root before mutation. The legacy.xls file is an explicit unsupported-format marker, not proof of a valid BIFF workbook. inert.xlsm contains a non-executing byte payload used to verify read-only preservation/rejection. The archive-bomb fixture is small and bounded; tests construct and reject it without general extraction.
