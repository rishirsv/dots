interface:
  display_name: "Repo Docs"
  short_description: "Write or update durable project docs"
  default_prompt: "Use $repo-docs to write or update a durable project doc such as README.md, AGENTS.md, ARCHITECTURE.md, or DESIGN.md, matching the existing voice."

policy:
  allow_implicit_invocation: false
