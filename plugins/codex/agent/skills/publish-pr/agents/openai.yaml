interface:
  display_name: "Publish PR"
  short_description: "Publish scoped work as a PR"
  default_prompt: "Use $publish-pr to publish the scoped local change as a pushed branch and PR; ask to publish and merge to mark it ready and merge or enable auto-merge."

policy:
  allow_implicit_invocation: false
