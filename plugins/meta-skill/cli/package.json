{
  "name": "@perks/meta-skill-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Meta Skill v1 TypeScript CLI for portable skill workbenches.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/**/*.test.js\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
