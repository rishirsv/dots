{
  "name": "@perks/meta-skill-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Meta Skill v1 TypeScript CLI for portable skill workbenches.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "rm -rf app && tsc -p tsconfig.json",
    "build:test": "rm -rf app && tsc -p tsconfig.test.json",
    "test": "npm run build:test && node --test \"app/**/*.test.js\" && npm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
