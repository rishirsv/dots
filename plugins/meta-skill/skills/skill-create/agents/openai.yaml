interface:
  display_name: "Skill Create"
  short_description: "Create reusable knowledge-work skills"
  default_prompt: "Use $skill-create to create a portable reusable skill, adding a .meta-skill workbench only when needed."
