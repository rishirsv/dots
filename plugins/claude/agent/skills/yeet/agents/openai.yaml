interface:
  display_name: "Yeet"
  short_description: "Alias for publishing a draft PR"
  default_prompt: "Use $yeet to publish the scoped local change as a pushed branch and draft PR."

policy:
  allow_implicit_invocation: false
