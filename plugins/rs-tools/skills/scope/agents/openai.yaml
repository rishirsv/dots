interface:
  display_name: "Scope"
  short_description: "Run deep pre-plan scoping before substantial work"
  default_prompt: "Use $scope to run a one-question-at-a-time scoping discussion until the next chat decision, artifact, plan, Codex goal, research move, or implementation move is genuinely clear."
