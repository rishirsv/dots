interface:
  display_name: "Eval Engineering"
  short_description: "Guide eval tasks, isolated runs, and human review"
  default_prompt: "Use $eval-engineering to inspect my inputs, establish where I want to start, and guide the next useful step in task design, isolated runs, human review, judging, or comparison. Reuse existing work and confirm the run plan before inference."
