# <Project Name>

## Description

<One short paragraph explaining what this project does, who it serves, and why it exists.>

## Quickstart

```sh
# install dependencies
<command>

# run locally
<command>
```

Success signal:

- <deterministic result, local route, log line, or test output>

## Common Workflows

| Workflow | Command | Success signal |
|---|---|---|
| Build | `<command>` | <result> |
| Test | `<command>` | <result> |
| Lint | `<command>` | <result> |
| Format | `<command>` | <result> |
| Run one package/module | `<command>` | <result> |

## Configuration

- Config location: `<path>`
- Required env vars: `<names>`
- Local defaults: `<path or note>`

## Where Docs Live

List only docs that exist.

- `AGENTS.md` — <agent/contributor operating contract>
- `ARCHITECTURE.md` — <system boundaries and invariants>
- `DESIGN.md` — <design-system tokens and UI rules>
- `<PRD path>` — <product scope and acceptance criteria>
- `<SPEC path>` — <system contract and requirements>
- `<PLAN path>` — <self-contained execution plan>
- `docs/` — <additional docs>

## Verification

```sh
<command>
```

Expected output:

- <result>
