# Configs

Repo-owned, non-secret config snapshots and notes.

- `raycast/`: Raycast export files and restore notes.
- `vscode/`: VS Code user settings, keybindings, and extension list.

Keep credentials, tokens, private machine state, and app caches out of this folder.
