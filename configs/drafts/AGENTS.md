# Drafts Config

This folder is repo-managed Drafts style source.

- Keep reusable style guides under `styles/`.
- Keep `style-library.json` as the minimal lookup index.
- Do not add evidence audits, extraction ledgers, source corpora, or message exports here.
- Use `scripts/sync-configs.sh --drafts-styles` to sync this folder into Codex homes.
